[package]
name = "hmc"
version = "0.1.0"
edition = "2024"

[lib]
name = "hmc"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
kuva = { version = "0.2.0", features = ["pdf"] }
